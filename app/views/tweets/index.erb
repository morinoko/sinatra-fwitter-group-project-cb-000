<h1>Tweets</h1>
<% if @tweets.count > 0 %>
    <% @tweets.each do |tweet| %>
      <p><a href="/tweets/<%= tweet.id %>"><%= tweet.content %></a> - <%= tweet.user.username %></p>
    <% end %>
<% else %>
  <p>No tweets have been made yet!</p>
<% end %>
